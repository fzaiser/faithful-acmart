%% mathfields — inline-math ($...$) in reference fields via the bst backend,
%% rendered as REAL Typst math: greek letters, ^/_ grouping, relations/operators,
%% \frac, \oplus, \mathbb (text gate's NFKC + whitespace-drop match LaTeX's
%% math-italic output), plus the `tex-render` override (composed with
%% default-tex-render) for custom commands (\widget, \RR). LaTeX twin of mathfields.typ.
\documentclass[acmsmall,nonacm]{acmart}
\pagestyle{empty}
\settopmatter{printacmref=false}
\newcommand{\widget}{W}
\newcommand{\RR}{\mathbb{R}}
\begin{document}
\thispagestyle{empty}% titleless body-only fixture: re-assert empty.

\section{Introduction}
Inline math in titles \cite{lam,greek,custom,adv}.

\bibliographystyle{ACM-Reference-Format}
\bibliography{mathfields}

\end{document}
