%% crossref — BibTeX crossref handling (field inheritance + min_crossrefs) plus the
%% organization->key label fallback and the per-entry distinctURL field, all via the
%% bst backend. xparent is crossref'd twice -> listed, children render "See [N]";
%% xlonely is crossref'd once -> not listed, xsolo inherits its fields and renders in
%% full. LaTeX twin of crossref.typ.
\documentclass[acmsmall,nonacm]{acmart}
\pagestyle{empty}
\settopmatter{printacmref=false}
\begin{document}
\thispagestyle{empty}% titleless body-only fixture: re-assert empty.

\section{Introduction}
Crossref + small bst features \cite{xchild1,xchild2,xsolo,prockey,durl}.

\bibliographystyle{ACM-Reference-Format}
\bibliography{crossref}

\end{document}
