[package]
name = "faithful-acmart"
version = "0.1.0"
entrypoint = "src/lib.typ"
authors = ["Fabian Zaiser <fzaiser@mit.edu>"]
license = "MIT AND MIT-0"
description = "Every ACM paper format, matching LaTeX acmart."
repository = "https://github.com/fzaiser/faithful-acmart"
keywords = [
    "acmart",
    "ACM",
    "Association for Computing Machinery",
    "LaTeX",
    "sigconf",
    "acmsmall",
]
categories = ["paper"]
disciplines = ["computer-science"]
# Minimum compiler: the math symbol names the .bib TeX layer emits (`partial`, `planck` for
# ℏ, `inter`, `plus.o`, ...) are the stable, non-deprecated names from Typst 0.14 on, and
# 0.14 is also where PDF accessibility tags start being emitted.
compiler = "0.14.0"
# Files kept in the development repo but omitted from the published Universe bundle:
# the LaTeX sources / validation harness / bundled dev fonts, contributor docs, and the
# ACM JDS logo (ACM's trademark — the acmcp format takes a user-supplied `acmcp-logo:`).
exclude = [
    "/.github",
    "/.venv",
    "/acmart",
    "/fonts",
    "/tests",
    "/tools",
    "/tmp",
    "/.gitignore",
    "/.python-version",
    "/CLAUDE.md",
    "/CONTRIBUTING.md",
    "/DESIGN.md",
    "/PUBLISHING.md",
    "/TODO.md",
    "/acmart-upstream-findings.md",
    "/port-audit-2026-07.md",
    "/pyproject.toml",
    "/uv.lock",
    "/src/README.md",
    "/src/assets/acm-jdslogo.png",
]

[template]
path = "template"
entrypoint = "main.typ"
thumbnail = "thumbnail.png"
